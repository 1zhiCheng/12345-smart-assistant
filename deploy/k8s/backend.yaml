# Orchestrator + API 全局服务（含 HPA）
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: wuhu-12345
spec:
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: wuhu-12345
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: wuhu-12345-agent:v1.0
          ports:
            - containerPort: 8000
          env:
            - name: STORAGE_MODE
              value: "mongo"
            - name: VECTOR_BACKEND
              value: "mongo"
            - name: DEPT_AGENTS_ENABLED
              value: "true"
            - name: UPLOAD_STORAGE_DIR
              value: "/data/uploads"
            - name: EMBEDDING_PROVIDER
              value: "local"
            - name: EMBEDDING_MODEL
              value: "BAAI/bge-small-zh-v1.5"
            - name: EMBEDDING_DIM
              value: "512"
            - name: DEEPSEEK_MODEL
              value: "deepseek-v4-flash"
            - name: DEEPSEEK_BASE_URL
              value: "https://api.deepseek.com"
            - name: RELAY_BASE_URL
              value: ""
            - name: DEEPSEEK_API_KEY
              valueFrom:
                secretKeyRef:
                  name: wuhu-12345-secrets
                  key: DEEPSEEK_API_KEY
            - name: RELAY_API_KEY
              valueFrom:
                secretKeyRef:
                  name: wuhu-12345-secrets
                  key: RELAY_API_KEY
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: wuhu-12345-secrets
                  key: MONGODB_URI
            - name: REDIS_ADDR
              valueFrom:
                secretKeyRef:
                  name: wuhu-12345-secrets
                  key: REDIS_ADDR
            - name: AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: wuhu-12345-secrets
                  key: AUTH_SECRET
            - name: INTERNAL_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: wuhu-12345-secrets
                  key: INTERNAL_API_TOKEN
            - name: SEED_DEMO_USERS
              value: "false"
            - name: SEED_OPERATIONAL_DEMO_DATA
              value: "false"
            - name: INTAKE_LLM_ENABLED
              value: "false"
            - name: BOOTSTRAP_ADMIN_USERNAME
              valueFrom:
                secretKeyRef:
                  name: wuhu-12345-secrets
                  key: BOOTSTRAP_ADMIN_USERNAME
            - name: BOOTSTRAP_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: wuhu-12345-secrets
                  key: BOOTSTRAP_ADMIN_PASSWORD
            - name: BOOTSTRAP_ADMIN_NAME
              valueFrom:
                secretKeyRef:
                  name: wuhu-12345-secrets
                  key: BOOTSTRAP_ADMIN_NAME
            - name: PI_AGENT_ENABLED
              value: "false"
            - name: PI_AGENT_URL
              value: "http://pi-agent:8100"
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "2000m"
              memory: "2Gi"
          volumeMounts:
            - name: uploads
              mountPath: /data/uploads
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: wuhu-12345-uploads
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: backend-hpa
  namespace: wuhu-12345
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
